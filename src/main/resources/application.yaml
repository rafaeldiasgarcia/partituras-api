spring:
  application:
    name: partituras

  datasource:
    url: jdbc:postgresql://localhost:5432/partituras
    username: postgres
    password: 1q2w3e

server:
  error:
    include-message: always

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    org.hibernate.SQL: DEBUG